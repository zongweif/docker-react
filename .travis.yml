sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t zongweif/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true zongweif/docker-react npm run test